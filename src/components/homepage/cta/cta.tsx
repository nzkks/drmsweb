import { Container } from '@/components';

const Cta = () => (
  <section className="py-16" id="certifications">
    <Container>
      <div>CTA</div>
    </Container>
  </section>
);

export default Cta;
